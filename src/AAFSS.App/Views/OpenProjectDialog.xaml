<Window x:Class="AAFSS.App.Views.OpenProjectDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="打开项目" Width="560" Height="420"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="最近打开的项目" FontWeight="SemiBold" FontSize="14"
                   Margin="0,0,0,12" />

        <!-- Recent Projects List -->
        <ListBox Grid.Row="1" x:Name="RecentList" Margin="0,0,0,8"
                 BorderBrush="LightGray">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="4,2">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="12" />
                            <TextBlock Text="{Binding FilePath}" FontSize="10" Foreground="Gray"
                                       TextTrimming="CharacterEllipsis" />
                        </StackPanel>
                        <TextBlock Grid.Column="1" Text="{Binding ModifiedAt, StringFormat='{}{0:yyyy-MM-dd HH:mm}'}"
                                   FontSize="10" Foreground="Gray" VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button Content="浏览..." Width="80" Height="28"
                    Click="OnBrowseClick" Margin="0,0,8,0" />
            <Button Content="打开" Width="80" Height="28"
                    Click="OnOpenClick" Margin="0,0,8,0" />
            <Button Content="取消" Width="80" Height="28"
                    IsCancel="True" Click="OnCancelClick" />
        </StackPanel>
    </Grid>
</Window>
